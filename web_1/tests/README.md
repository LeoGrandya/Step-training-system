web_1 tests
===========

This directory holds backend contract tests for the Flask application. Tests here
must use the standard library unless project dependencies are explicitly
installed for the test environment.
