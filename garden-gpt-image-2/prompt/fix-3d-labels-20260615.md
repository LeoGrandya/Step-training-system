## 任务

以 REFERENCE_0（图片1.png）为基础，将图中的两处文字标签替换为准确反映项目真实情况的文字。

### 替换 1
- **原文**: `3D渲染数据`
- **改为**: `3D 重建数据`
- **原因**: 本项目使用 MediaPipe + 双目三角测量做 3D 姿态重建（reconstruction），不是 3D 渲染（rendering）

### 替换 2
- **原文**: `实时3D模型`
- **改为**: `3D 骨骼模型`
- **原因**: 本系统是离线批处理流水线，不提供实时处理；产物是 3D 骨骼关键点数据，不是交互式 3D 模型

### 约束
- 保持原图中字体的风格、大小、颜色不变
- 保持文字所在背景区域（绿色/粉色色块）不变
- 仅替换文字内容本身，周围元素完全保留
- 新文字需要在原位置居中显示
- 如果是按钮/圆角矩形上的文字，保持文字在矩形内的位置不变

### 图像上下文
该图是一个系统架构图，包含前端、后端、算法、数据等多个层级（用不同颜色区分）。文字标签位于架构图的特定层中。
