# -----------------------------------------------------------------------------
# 依赖分层说明
# - web_1/requirements.txt  主站（Flask + 视频分析）最小可运行集合；日常开发优先装它。
# - 本文件（根目录）        全仓库/多子项目合并口径（含 video_sync、运动学脚本、Streamlit 工具等）。
# -----------------------------------------------------------------------------

Flask>=3.1,<4.0
Werkzeug>=3.1,<4.0
Flask-SQLAlchemy>=3.1,<4.0
PyMySQL>=1.1,<2.0
alembic>=1.13,<2.0
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7

opencv-python>=4.8
mediapipe>=0.10
Pillow>=10.0

PyYAML>=6.0
tqdm>=4.60

# web_1 主站已使用（根列表补齐，避免「只装根依赖」却缺包）
openpyxl>=3.1.5
pyserial>=3.5
